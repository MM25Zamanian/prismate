"use client";

// Optional manual registration helper (not used if register=true)
export default function RegisterPWA() {
  // English comment: keep this as a helper if auto register is disabled
  return null;
}

